# Эволюция проекта: переход на TypeScript

## Цель
Мигрировать проект на TypeScript

## Задание
1. установите в проект `typescript` (`TS`) с помощью `npm i -D typescript`
2. проинициализируйте конфиг `typescript`. `npx tsc --init`
3. добавьте поддержку `TS` в инструменты, что используете на проекте. Пример: `jest` / `eslint` / `prettier`
4. добавьте в `package.json ` скрипт для проверки типов (`tsc --noEmit`). Рекомендованное имя: `type-check`
5. переведите весь проект в `TS`, файлы конфигурации инструментов (`jest` / `eslint` / ...) не обязательно, должны быть преобразованы на `TS`

💡**Рекомендации:**

* [ts-migrate](https://www.npmjs.com/package/ts-migrate) -- преобразование `JS` в `TS`

---
## Критерии оценивания
* `npx tsc --noEmit` не выдает ошибок
* линтер / форматтер поддерживают TS
* в `package.json` добавил скрипт для проверки типов (`tsc --noEmit`)
 