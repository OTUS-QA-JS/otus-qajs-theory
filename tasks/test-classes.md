# Эволюция кода: преобразуем Page Object в классы и не только

## Цель
Применить классы на практике. Сравнить реализацию POM (page object model) через функцию с реализацией через класс.

## Задание
Возьмите за основу код после ДЗ по применению POM (page object model), перепишите POM на классы. 

*Опционально*
* Напишите, какая реализация POM вам больше нравится. Через функцию или через класс.
* Напишите больше тестовых сценариев 
* Примените `class` вместо `function`, там где это по вашему сделает код проще в поддержке. Например, в сервисах. 

💡**Рекомендации:**
В работе выберете один подход для POM. Не стоит реализовывать один POM через `class`, а другой через `function`, лучше поддерживаться одного подхода, для упрощения поддержки проекта.

---
## Критерии оценивания
* есть Page Object с использованием класса